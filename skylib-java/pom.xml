<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.1.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 http://maven.apache.org/xsd/maven-4.1.0.xsd">
	<modelVersion>4.1.0</modelVersion>

  <groupId>ch.scaille</groupId>
	<artifactId>libs</artifactId>
	<version>${revision}</version>
	<name>libs</name>
	<packaging>pom</packaging>
	<subprojects>
		<subproject>lib-testing</subproject>
		<subproject>lib-annotations</subproject>
		<subproject>lib-utils</subproject>
		<subproject>lib-javabeans</subproject>
		<subproject>lib-persistence</subproject>
		<subproject>lib-gui</subproject>
		<subproject>lib-gui-generators</subproject>
		<subproject>lib-gui-validation</subproject>
		<subproject>lib-gui-examples</subproject>
		<subproject>lib-generator-utils</subproject>
	</subprojects>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>21</java.version>
		<maven.compiler.source>${java.version}</maven.compiler.source>
		<maven.compiler.target>${java.version}</maven.compiler.target>
	</properties>
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-enforcer-plugin</artifactId>
				<executions>
					<execution>
						<id>enforce-versions</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>3.7</version>
								</requireMavenVersion>
								<requireJavaVersion>
									<version>${java.version}</version>
								</requireJavaVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>org.jetbrains</groupId>
			<artifactId>annotations</artifactId>
		</dependency>
	</dependencies>
	
</project>
