get:
  summary: List all test cases
  operationId: listAll
  responses:
    '200':
      description: all  test cases
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../openapi.yaml#/components/schemas/Metadata
