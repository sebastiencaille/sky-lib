components:
  schemas:
    TestDictionary:
      $ref: ../openapi.yaml#/components/schemas/TestDictionary

get:
  summary: Gets the currently selected dictionary
  operationId: current
  responses:
    '200':
      description: current dictionaries
      content:
        application/json:
          schema:
            $ref: TestDictionary
