components:
  schemas:
    TestDictionary:
      $ref: ../openapi.yaml#/components/schemas/TestDictionary
get:
  summary: Get current dictionary
  operationId: current
  responses:
    '200':
      description: current dictionaries
      content:
        application/json:
          schema:
            $ref: TestDictionary
